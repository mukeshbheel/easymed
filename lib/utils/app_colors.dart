import 'package:flutter/material.dart';

class AppColors {
  AppColors._(); // Prevent instantiation

  // Primary / Backgrounds
  static const Color appBarBackground = Color(0xFFfbeee5);
  static const Color scaffoldBackground = Color(0xFFFFFFFF);

  // Text
  static const Color primaryText = Colors.black;
  static const Color secondaryText = Color(0xFF6B6B6B);

  // Borders & Dividers
  static const Color divider = Color(0xFFE0E0E0);

  // Buttons / Accents
  static const Color primary = Color(0xFFEF6C00);
  static const Color secondary = Color(0xFFFFB74D);

  // Status
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFFA000);
  static const Color error = Color(0xffe21c41);
}
